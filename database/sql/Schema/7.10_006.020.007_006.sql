IF EXISTS(
  SELECT * 
  FROM 
    SYS.TABLES tbl,
    SYS.PARTITIONS prt
  WHERE tbl.OBJECT_ID = prt.OBJECT_ID 
    AND tbl.OBJECT_ID = OBJECT_ID(N'[PTY_IMPL].[PARTY_INFO_SAT]'))
BEGIN
  IF COL_LENGTH('PTY_IMPL.PARTY_INFO_SAT','NON_RESIDENT_CODE') IS NULL
  BEGIN
    ALTER TABLE PTY_IMPL.PARTY_INFO_SAT ADD NON_RESIDENT_CODE NVARCHAR(256) NULL
  END
END

GO