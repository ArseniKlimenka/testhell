IF EXISTS (SELECT * FROM SYS.OBJECTS WHERE OBJECT_ID = OBJECT_ID(N'[PAS_IMPL].[EFR_PRODUCT_RISK]') AND TYPE IN (N'U'))
BEGIN

IF COL_LENGTH('PAS_IMPL.EFR_PRODUCT_RISK','PRODUCT_TYPE_ADINSURE') IS NULL
BEGIN
ALTER TABLE PAS_IMPL.EFR_PRODUCT_RISK ADD PRODUCT_TYPE_ADINSURE nvarchar(max)
END

END
GO

UPDATE PAS_IMPL.EFR_PRODUCT_RISK 
SET PRODUCT_TYPE_ADINSURE = (
    CASE  
        WHEN (PRODUCT_TYPE_SAP = N'НСЖ') THEN 'AccumulatedLifeInsurance' 
        WHEN (PRODUCT_TYPE_SAP = N'ИСЖ') THEN 'InvestmentLifeInsurance' 
    END 
)