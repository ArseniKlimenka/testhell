IF EXISTS (SELECT 1 FROM sys.objects WHERE OBJECT_ID = OBJECT_ID(N'[PAS_IMPL].[PAYMENT_TYPE]') AND TYPE IN (N'U'))
BEGIN
	IF NOT EXISTS (SELECT 1 FROM PAS_IMPL.PAYMENT_TYPE WHERE PAYMENT_TYPE_CODE = 5)
	BEGIN
		INSERT INTO PAS_IMPL.PAYMENT_TYPE
		(
			PAYMENT_TYPE_CODE,
			PAYMENT_TYPE_DESCRIPTION
		)
		VALUES (
			5,
			N'ДИД СЛП'
		)
	END
END