IF NOT EXISTS (SELECT * FROM SYS.OBJECTS WHERE OBJECT_ID = OBJECT_ID(N'[PAS_IMPL].[P_INVOICED_COMMISSION]') AND TYPE IN (N'U'))
BEGIN
	CREATE TABLE [PAS_IMPL].[P_INVOICED_COMMISSION]
	(
		[INVOICED_COMMISSION_ID] [uniqueidentifier] NOT NULL,
		[LOAD_DATE] [datetime2](7) NULL,
		[CONTRACT_NUMBER] [nvarchar](50) NULL,
		[DUE_DATE] [date] NULL,
		[POSTING_DATE] [date] NULL,
		[OBJECT_CODE] [nvarchar](128) NULL,
		[ITEM_NO] [nvarchar](50) NULL,
		[CURRENCY_CODE] [varchar](3) NULL,
		[BASE_AMOUNT] [decimal](15, 2) NULL,
		[AA_COMM_RATE] [decimal](15, 6) NULL,
		[DOC_COMM_RATE] [decimal](15, 6) NULL,
		[CALC_COMM_AMOUNT] [decimal](15, 2) NULL,
		[COMM_TYPE] [int] NULL,
		CONSTRAINT [PK_PAS_IMPL_P_INVOICED_COMMISSION] PRIMARY KEY CLUSTERED ([INVOICED_COMMISSION_ID] ASC)
	);
END
GO

update pas_impl.P_INVOICED_COMMISSION set COMM_TYPE = 1 where COMM_TYPE is null
go

alter table pas_impl.P_INVOICED_COMMISSION alter column COMM_TYPE int null
go
