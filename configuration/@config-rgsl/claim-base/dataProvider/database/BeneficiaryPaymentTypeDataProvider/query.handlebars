SELECT ct.CODE,
	   ct.DESCRIPTION
FROM BFX_IMPL.BENEFICIARY_PAYMENT_TYPE ct
WHERE 1 = 1
{{#if parameters.code}}
AND ct.CODE = @code
{{/if}}
{{#if parameters.codes}}
AND ct.CODE IN (@codes)
{{/if}}
{{#if parameters.description}}
AND ct.DESCRIPTION LIKE @description
{{/if}}
ORDER BY ct.CODE ASC