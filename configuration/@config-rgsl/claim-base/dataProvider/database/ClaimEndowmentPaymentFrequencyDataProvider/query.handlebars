SELECT CODE, DESCRIPTION
FROM BFX_IMPL.PAYMENT_FREQUENCY ct
WHERE 1 = 1
{{#if parameters.code}}
AND ct.CODE = @code
{{/if}}
{{#if parameters.typeCode}}
AND ct.TYPE_CODE = @typeCode
{{/if}}
{{#if parameters.description}}
AND ct.DESCRIPTION LIKE @description
{{/if}}
ORDER BY ct.CODE ASC