const assert = require('assert');

describe("Agent Agreement", function() {

    it('Empty test so it does not fail with no tests', function() {
        assert.ok(true, 'true is indeed true');
    });
});
