'use strict';

const DateTimeUtils = require('@config-rgsl/infrastructure/lib/DateTimeUtils');

module.exports = function partnerFilter(input, ambientProperties) {

    const availablePartners = input.items;

    return availablePartners;
};
