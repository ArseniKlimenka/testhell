'use strict';

module.exports = function showCumulationForPolicyHolder(input, ambientProperties) {

    /* const isPolicyHolder = input.context?.Body?.insuredPerson?.isPolicyHolder;
    return !isPolicyHolder;*/

    // hide while no products with this tab
    return false;
};
