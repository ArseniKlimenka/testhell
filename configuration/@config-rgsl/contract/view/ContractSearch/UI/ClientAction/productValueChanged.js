'use strict';

const { getValue } = require('@config-rgsl/infrastructure/lib/ObjectUtils');

module.exports = function productValueChanged(input) {

    null;
    // input.context.request.data.criteria.productGroup = getValue(input, 'data.product.productGroup', undefined);

};
