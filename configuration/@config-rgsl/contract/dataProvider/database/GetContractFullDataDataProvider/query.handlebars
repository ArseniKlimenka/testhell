SELECT c.CONTRACT_NUMBER AS AMENDMENT_NUMBER,
       c.CONTRACT_ID,
       c.BODY,
       c.COMMON_BODY,
       art.CODE_NAME AS CONF_NAME
FROM pas.CONTRACT c
JOIN cfx.PUBLISHED_ARTIFACT art ON art.PUBLISHED_ARTIFACT_ID = c.PUBLISHED_ARTIFACT_ID
WHERE c.CONTRACT_NUMBER = @contractNumber