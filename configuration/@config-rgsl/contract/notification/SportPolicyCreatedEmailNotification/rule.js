'use strict';

module.exports = function rule(input) {

    const items = [
        {
            staticAsset: {
                name: 'Logo.png'
            }
        },
        {
            staticAsset: {
                name: 'leg_doc.png'
            }
        }
    ];

    return items;
};
