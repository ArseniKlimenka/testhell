SELECT 
	SAD_NUMBER,
	SAD_NUMBER_1,
	SAD_NUMBER_2,
	SAD_NUMBER_MAG,
	SAD_NUMBER_NSO
FROM ORG_IMPL.SP_SUB_AGENT_SAT_LATEST spsas
	INNER JOIN org_impl.SERVICE_PROVIDER_HUB in_sph on spsas.SP_SUB_AGENT_HKEY = in_sph.SERVICE_PROVIDER_HKEY
WHERE in_sph.SERVICE_PROVIDER_CODE = @spCode